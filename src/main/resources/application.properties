# ===== Database =====
# ===== Database (Local) =====
spring.datasource.url=jdbc:mysql://localhost:3306/helpdesk_project_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=20272405
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===== Database (Railway/Render - Uncomment for Prod) =====
# spring.datasource.url=${DB_URL}
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}

# ===== JPA / Hibernate =====
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===== Server =====
server.port=${PORT:8080}

# ===== JWT =====
app.jwt.secret=${JWT_SECRET:MyHelpdeskSuperSecretKeyForJWTTokenGenerationAndValidation2026!@#}
app.jwt.expiration-ms=86400000

# ===== Mail (SMTP) =====
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ===== Static Resources ===== 
spring.web.resources.static-locations=classpath:/static/
